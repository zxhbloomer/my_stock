# 🚀 My Stock 启动指南

## 第一步：创建 Conda 虚拟环境

### 方式1：使用脚本（推荐）

双击运行：
```
scripts\setup_env.bat
```

### 方式2：手动创建

在项目根目录打开命令行：
```bash
# 创建环境
conda env create -f environment.yml

# 激活环境
conda activate mystock
```

---

## 第二步：激活环境

**每次使用前都需要激活环境！**

```bash
conda activate mystock
```

---

## 第三步：下载数据

### 方式1：使用脚本（推荐）

激活环境后，双击运行：
```
scripts\download_data.bat
```

### 方式2：手动下载

```bash
conda activate mystock
python -m qlib.run.get_data qlib_data --target_dir ~/.qlib/qlib_data/cn_data --region cn
```

**数据下载需要时间，请耐心等待！**

---

## 第四步：运行第一个示例

### 方式1：使用配置文件（推荐）

```bash
conda activate mystock
qrun configs\workflow_config_lightgbm_Alpha158.yaml
```

或双击：
```
scripts\run_example.bat
```

### 方式2：使用 Jupyter Notebook

```bash
conda activate mystock
jupyter notebook notebooks\01_workflow_by_code.ipynb
```

---

## 第五步：查看结果

运行完成后：

```bash
conda activate mystock
mlflow ui
```

然后在浏览器打开：http://localhost:5000

---

## 📋 完整流程总结

```bash
# 1. 创建环境（只需一次）
conda env create -f environment.yml

# 2. 激活环境（每次使用）
conda activate mystock

# 3. 下载数据（只需一次）
python -m qlib.run.get_data qlib_data --target_dir ~/.qlib/qlib_data/cn_data --region cn

# 4. 运行策略
qrun configs\workflow_config_lightgbm_Alpha158.yaml

# 5. 查看结果
mlflow ui
```

---

## ⚙️ 环境管理命令

```bash
# 查看所有环境
conda env list

# 激活环境
conda activate mystock

# 退出环境
conda deactivate

# 删除环境（如果需要重新创建）
conda env remove -n mystock

# 导出环境（用于分享）
conda env export > environment_export.yml
```

---

## 🔍 验证安装

激活环境后，运行：

```bash
conda activate mystock

# 验证 Python 版本
python --version
# 应该显示: Python 3.8.x

# 验证 Qlib 安装
python -c "import qlib; print(qlib.__version__)"
# 应该显示版本号

# 验证依赖包
python -c "import pandas, numpy, sklearn; print('OK')"
# 应该显示: OK
```

---

## ❓ 常见问题

### Q1: conda 命令不存在？
**A**: 需要先安装 Anaconda 或 Miniconda
- Anaconda: https://www.anaconda.com/download
- Miniconda: https://docs.conda.io/en/latest/miniconda.html

### Q2: 环境创建失败？
**A**: 检查：
1. environment.yml 文件是否存在
2. 网络连接是否正常
3. conda 版本是否过旧（建议更新: `conda update conda`）

### Q3: 数据下载很慢或失败？
**A**:
1. 检查网络连接
2. 可以多试几次
3. 参考 `scripts/README.md` 使用其他下载方式

### Q4: 运行报错 "qlib not initialized"？
**A**:
1. 确保已下载数据
2. 检查配置文件中的 provider_uri 路径
3. 确保环境已激活

---

## 📚 下一步

环境搭建完成后，可以：

1. **学习数据探索**
   - 运行 `notebooks/02_data_exploration.ipynb`

2. **测试不同策略**
   - CSI300: `qrun configs/workflow_config_lightgbm_Alpha158.yaml`
   - CSI500: `qrun configs/workflow_config_lightgbm_Alpha158_csi500.yaml`

3. **修改配置文件**
   - 复制配置文件
   - 修改市场、时间范围、模型参数
   - 运行自己的策略

4. **查看官方示例**
   - 参考 `examples/README.md`
   - 访问 Qlib 官方仓库

---

**祝您使用愉快！**
