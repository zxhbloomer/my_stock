# v4_plan_1 卖出逻辑增强 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 v4_plan 代码复制到 v4_plan_1，并移植 v3 的完整卖出逻辑（硬止损 + ATR 追踪止损 + BBI 死叉 + MACD 死叉 + 筹码胜率），实现逐日止损检查，同时保留原有周度轮动换仓逻辑。

**Architecture:** 在 v4_plan 的纯 pandas 手写回测框架基础上，新增逐日止损检查循环（在周一换仓之前执行）。数据层在 10_prepare_data.py 中预计算 ATR14、MACD、MACD_Signal 三列，回测层扩展 holdings 字典以维护每只持仓的追踪止损状态。止损卖出后当天不买入，等下周一换仓时资金自动参与分配。

**Tech Stack:** Python 3.x, pandas, sqlalchemy, parquet

---

## 文件结构

| 文件 | 操作 |
|------|------|
| `scripts/bbi/backtrader/v4_plan_1/config.py` | 从 v4_plan 复制，新增止损参数 |
| `scripts/bbi/backtrader/v4_plan_1/10_prepare_data.py` | 从 v4_plan 复制，新增 ATR14/MACD/MACD_Signal 计算 |
| `scripts/bbi/backtrader/v4_plan_1/20_run_backtest.py` | 从 v4_plan 复制，新增逐日止损逻辑 |
| `scripts/bbi/backtrader/v4_plan_1/README.md` | 新建，说明与 v4_plan 的差异 |

---

## Task 1: 创建目录并复制基础文件

**Files:**
- Create: `scripts/bbi/backtrader/v4_plan_1/config.py`
- Create: `scripts/bbi/backtrader/v4_plan_1/10_prepare_data.py`
- Create: `scripts/bbi/backtrader/v4_plan_1/20_run_backtest.py`

- [ ] **Step 1: 创建 v4_plan_1 目录，复制 config.py**

将 v4_plan/config.py 内容复制到 v4_plan_1/config.py，并新增以下参数：

```python
from pathlib import Path

START_DATE = "2018-01-01"
END_DATE   = None  # None = today

FILTER_MIN_LIST_DAYS  = 365
FILTER_MIN_CIRC_MV    = 1_000_000  # 万元 = 100亿元
FILTER_MIN_AMOUNT     = 50_000     # 千元 = 5000万元

DB_URL = "postgresql://root:123456@localhost:5432/my_stock"
SCHEMA = "tushare_v2"

BBI_PERIODS = (5, 10, 20, 60)

# 轮动策略参数
INIT_CASH   = 500_000.0   # 初始资金 50万
TOP_N       = 5           # 每周持仓只数
COMM_BUY    = 0.0005
COMM_SELL   = 0.0015
MIN_COMM    = 5.0
RISK_FREE   = 0.02

# 止损参数（从 v3 移植）
ATR_PERIOD          = 14
ATR_MULTIPLIER      = 4.5    # peak_close - 4.5 * ATR14
HARD_STOP_LOSS      = 0.08   # 亏损 8% 硬止损
CHIP_EXIT_THRESHOLD = 80.0   # winner_rate > 80 触发筹码止损
MIN_HOLD_DAYS       = 20     # 信号类止损最小持仓天数

OUTPUT_DIR     = Path(__file__).parent / "output"
STOCK_DATA_DIR = OUTPUT_DIR / "stock_data"
```

- [ ] **Step 2: 验证 config.py 语法**

```bash
python -X utf8 -c "import sys; sys.path.insert(0, 'scripts/bbi/backtrader/v4_plan_1'); import config; print('OK', config.INIT_CASH)"
```

Expected: `OK 500000.0`

---

## Task 2: 修改 10_prepare_data.py — 新增技术指标列

**Files:**
- Create: `scripts/bbi/backtrader/v4_plan_1/10_prepare_data.py`

- [ ] **Step 1: 复制 v4_plan/10_prepare_data.py 到 v4_plan_1/，修改 import**

文件头部 import 改为从本目录 config 导入：

```python
# v4_plan_1/10_prepare_data.py
# 准备轮动策略所需的 parquet 数据
# 在 v4_plan 基础上新增 ATR14、MACD、MACD_Signal 预计算列
import shutil
import datetime
import pandas as pd
from sqlalchemy import create_engine, text
from config import (
    DB_URL, START_DATE, END_DATE,
    FILTER_MIN_CIRC_MV, FILTER_MIN_AMOUNT,
    STOCK_DATA_DIR, OUTPUT_DIR,
    BBI_PERIODS, ATR_PERIOD,
)
```

- [ ] **Step 2: 在 Step 4 的逐股处理循环中，BBI 计算之后新增技术指标计算**

在 `df['bbi_qfq'] = df[ma_cols].mean(axis=1)` 之后，`df = df.dropna(subset=['bbi_qfq'])` 之前，插入：

```python
        # ATR14 (Wilder 平滑)
        high = df['high_qfq']
        low  = df['low_qfq']
        prev_close = df['close_qfq'].shift(1)
        tr = pd.concat([
            high - low,
            (high - prev_close).abs(),
            (low  - prev_close).abs(),
        ], axis=1).max(axis=1)
        # Wilder 平滑：alpha = 1/ATR_PERIOD
        df['atr14'] = tr.ewm(alpha=1.0 / ATR_PERIOD, adjust=False).mean()

        # MACD (EMA12 - EMA26) 和 Signal (EMA9 of MACD)
        ema12 = df['close_qfq'].ewm(span=12, adjust=False).mean()
        ema26 = df['close_qfq'].ewm(span=26, adjust=False).mean()
        df['macd']        = ema12 - ema26
        df['macd_signal'] = df['macd'].ewm(span=9, adjust=False).mean()
```

- [ ] **Step 3: 更新 to_parquet 调用前确认列存在**

`df.to_parquet(...)` 这行不需要改，新列会自动包含在 parquet 中。

完整的逐股处理块（替换原有的 for 循环体）：

```python
    skipped = 0
    for i, row in df_stocks.iterrows():
        ts_code = row["ts_code"]
        for attempt in range(3):
            try:
                with engine.connect() as conn:
                    df = _query(conn, sql_data, {
                        "ts_code": ts_code,
                        "start_date": START_DATE,
                        "end_date": end_date,
                    })
                break
            except Exception as e:
                if attempt == 2:
                    print(f"  SKIP {ts_code} after 3 attempts: {e}")
                    df = pd.DataFrame()
                    break
        if df.empty or len(df) < max(BBI_PERIODS) + 10:
            skipped += 1
            continue

        for p in BBI_PERIODS:
            df[f'ma{p}'] = df['close_qfq'].rolling(p).mean()
        ma_cols = [f'ma{p}' for p in BBI_PERIODS]
        df['bbi_qfq'] = df[ma_cols].mean(axis=1)
        df = df.drop(columns=[f'ma{p}' for p in BBI_PERIODS if p != 60])

        # ATR14 (Wilder 平滑)
        high = df['high_qfq']
        low  = df['low_qfq']
        prev_close = df['close_qfq'].shift(1)
        tr = pd.concat([
            high - low,
            (high - prev_close).abs(),
            (low  - prev_close).abs(),
        ], axis=1).max(axis=1)
        df['atr14'] = tr.ewm(alpha=1.0 / ATR_PERIOD, adjust=False).mean()

        # MACD 和 Signal
        ema12 = df['close_qfq'].ewm(span=12, adjust=False).mean()
        ema26 = df['close_qfq'].ewm(span=26, adjust=False).mean()
        df['macd']        = ema12 - ema26
        df['macd_signal'] = df['macd'].ewm(span=9, adjust=False).mean()

        df = df.dropna(subset=['bbi_qfq'])
        if len(df) < 10:
            skipped += 1
            continue

        df['trade_date'] = pd.to_datetime(df['trade_date'])
        if ts_code in mf_dict:
            df = df.merge(mf_dict[ts_code], on='trade_date', how='left')
        else:
            df['lg_net_vol'] = float('nan')
        if ts_code in cyq_dict:
            df = df.merge(cyq_dict[ts_code], on='trade_date', how='left')
        else:
            df['winner_rate'] = float('nan')

        # 盘后数据 T-1 shift，防止未来数据泄露
        df['lg_net_vol']  = df['lg_net_vol'].shift(1)
        df['winner_rate'] = df['winner_rate'].shift(1)

        df["name"] = row["name"]
        df.to_parquet(STOCK_DATA_DIR / f"{ts_code}.parquet", index=False)
        if (i + 1) % 100 == 0:
            print(f"  {i + 1}/{len(df_stocks)} done...")
```

---

## Task 3: 修改 20_run_backtest.py — 新增逐日止损逻辑

**Files:**
- Create: `scripts/bbi/backtrader/v4_plan_1/20_run_backtest.py`

- [ ] **Step 1: 复制 v4_plan/20_run_backtest.py，修改 import**

```python
# v4_plan_1/20_run_backtest.py
# BBI 周度轮动策略回测 — 增强版
# 在 v4_plan 基础上新增逐日止损检查：
#   硬止损 8% / ATR 追踪止损 / BBI 死叉 / MACD 死叉 / 筹码胜率过高
import json
import csv
import pandas as pd
import numpy as np
from pathlib import Path
from config import (
    START_DATE, END_DATE,
    INIT_CASH, TOP_N,
    COMM_BUY, COMM_SELL, MIN_COMM,
    STOCK_DATA_DIR, OUTPUT_DIR,
    ATR_MULTIPLIER, HARD_STOP_LOSS,
    CHIP_EXIT_THRESHOLD, MIN_HOLD_DAYS,
)
import datetime
```

- [ ] **Step 2: 新增 check_stop_loss 函数**

在 `calc_comm` 函数之后，`load_stocks` 函数之前插入：

```python
def check_stop_loss(code, date, df_indexed, pos):
    """
    检查持仓是否触发止损。
    返回 (triggered: bool, reason: str)
    df_indexed: 以 trade_date 为索引的 DataFrame
    pos: holdings[code] 字典（会被原地修改以更新追踪止损状态）
    """
    if date not in df_indexed.index:
        return False, None

    row = df_indexed.loc[date]
    close = float(row['close_qfq'])
    cost  = pos['cost_price']

    # 更新追踪止损峰值
    if close > pos['peak_close']:
        pos['peak_close'] = close
    atr = row.get('atr14', float('nan'))
    if pd.notna(atr) and atr > 0:
        new_stop = pos['peak_close'] - ATR_MULTIPLIER * atr
        pos['trail_stop'] = max(pos['trail_stop'], new_stop)

    # 1. 硬止损（无持仓天数限制）
    if (close - cost) / cost <= -HARD_STOP_LOSS:
        return True, '硬止损'

    # 2. ATR 追踪止损（无持仓天数限制）
    if pos['trail_stop'] > 0 and close < pos['trail_stop']:
        return True, 'ATR追踪止损'

    # 信号类止损需持仓 >= MIN_HOLD_DAYS
    if pos['hold_days'] < MIN_HOLD_DAYS:
        return False, None

    # 3. BBI 死叉
    bbi_now = float(row['bbi_qfq'])
    prev_dates = df_indexed.index[df_indexed.index < date]
    if len(prev_dates) >= 1:
        prev_row   = df_indexed.loc[prev_dates[-1]]
        prev_close = float(prev_row['close_qfq'])
        prev_bbi   = float(prev_row['bbi_qfq'])
        if prev_close > prev_bbi and close < bbi_now:
            return True, 'BBI死叉'

    # 4. MACD 死叉 + BBI 3日下行
    macd   = row.get('macd', float('nan'))
    signal = row.get('macd_signal', float('nan'))
    if pd.notna(macd) and pd.notna(signal) and macd < signal and macd < 0:
        if len(prev_dates) >= 3:
            bbi_3d_ago = float(df_indexed.loc[prev_dates[-3]]['bbi_qfq'])
            if bbi_now < bbi_3d_ago:
                return True, 'MACD死叉'

    # 5. 筹码胜率过高（winner_rate 已在 prepare_data 中 shift(1)，是 T-1 数据）
    wr = row.get('winner_rate', float('nan'))
    if pd.notna(wr) and wr > CHIP_EXIT_THRESHOLD:
        return True, '筹码胜率过高'

    return False, None
```

- [ ] **Step 3: 修改 load_stocks，构建 df_indexed 字典**

在 `load_stocks` 函数中，`data[code] = df` 之后新增一行，返回时同时返回 `data_indexed`：

```python
def load_stocks():
    stock_list_path = OUTPUT_DIR / "stock_list.csv"
    stock_list = pd.read_csv(stock_list_path)
    valid = set(stock_list[~stock_list['ts_code'].str.startswith('688')]['ts_code'])
    name_map = dict(zip(stock_list['ts_code'], stock_list['name']))

    end_date = END_DATE or datetime.date.today().strftime("%Y-%m-%d")
    data = {}
    data_indexed = {}  # 以 trade_date 为索引，供 check_stop_loss 快速查询
    for f in STOCK_DATA_DIR.glob('*.parquet'):
        code = f.stem
        if code not in valid:
            continue
        df = pd.read_parquet(f)
        df['trade_date'] = pd.to_datetime(df['trade_date'])
        df = df.sort_values('trade_date').reset_index(drop=True)
        df = df[(df['trade_date'] >= START_DATE) & (df['trade_date'] <= end_date)]
        if len(df) < 60:
            continue
        data[code] = df
        data_indexed[code] = df.set_index('trade_date')
    print(f'Loaded {len(data)} stocks')
    return data, data_indexed, name_map
```

- [ ] **Step 4: 修改 run_backtest 函数签名，接收 data_indexed**

```python
def run_backtest(data, data_indexed, panel, name_map):
```

- [ ] **Step 5: 扩展 holdings 字典结构，新增止损状态字段**

在买入时（`holdings[code] = {...}` 处）新增字段：

```python
                holdings[code] = {
                    'shares': shares, 'cost_price': price,
                    'buy_date': str(date)[:10], 'name': name_map.get(code, code),
                    'peak_close': price,   # 追踪止损：初始峰值 = 买入价
                    'trail_stop': 0.0,     # 追踪止损线，0 表示未激活
                    'hold_days': 0,        # 持仓天数
                }
```

- [ ] **Step 6: 在主循环中新增逐日止损检查和 hold_days 更新**

在 `for date in all_dates:` 循环体最开始（`if date in monday_set:` 之前）插入：

```python
        # 逐日止损检查（非周一也执行）
        for code in list(holdings.keys()):
            pos = holdings[code]
            pos['hold_days'] += 1  # 每日递增持仓天数

            if code not in data_indexed:
                continue
            triggered, reason = check_stop_loss(code, date, data_indexed[code], pos)
            if not triggered:
                continue

            # 止损卖出
            if code not in day_panel.index:
                continue
            price = float(day_panel.loc[code, 'close_qfq'])
            shares = pos['shares']
            proceeds = price * shares
            comm = calc_comm(proceeds, False)
            pnl = proceeds - comm - pos['cost_price'] * shares
            pnl_pct = pnl / (pos['cost_price'] * shares) * 100
            cash += proceeds - comm
            trade_records.append({
                'date': str(date)[:10], 'ts_code': code,
                'name': name_map.get(code, code),
                'action': f'止损卖出({reason})',
                'price': round(price, 3), 'shares': shares,
                'amount': round(proceeds, 0), 'comm': round(comm, 1),
                'pnl': round(pnl, 0), 'pnl_pct': round(pnl_pct, 2),
            })
            del holdings[code]
```

- [ ] **Step 7: 修改 main 函数，传入 data_indexed**

```python
def main():
    OUTPUT_DIR.mkdir(parents=True, exist_ok=True)
    data, data_indexed, name_map = load_stocks()
    panel = build_panel(data)

    nav_series, weekly_records, trade_records, last_holdings = run_backtest(
        data, data_indexed, panel, name_map
    )
    # ... 其余保存逻辑不变
```

---

## Task 4: 新建 README.md

**Files:**
- Create: `scripts/bbi/backtrader/v4_plan_1/README.md`

- [ ] **Step 1: 创建 README.md**

```markdown
# BBI 周度轮动策略 v4_plan_1

## 与 v4_plan 的差异

v4_plan_1 在 v4_plan 的基础上，新增了从 v3 移植的完整卖出逻辑。

### 新增卖出条件（按优先级）

| 优先级 | 名称 | 触发条件 | 持仓天数限制 |
|--------|------|----------|-------------|
| 1 | 硬止损 | 亏损 ≥ 8% | 无 |
| 2 | ATR 追踪止损 | 收盘 < 历史峰值 - 4.5×ATR14 | 无 |
| 3 | BBI 死叉 | 前日收盘 > BBI，当日收盘 < BBI | ≥ 20 日 |
| 4 | MACD 死叉 | MACD < Signal 且 MACD < 0 且 BBI 3日下行 | ≥ 20 日 |
| 5 | 筹码胜率过高 | winner_rate（T-1）> 80 | ≥ 20 日 |

原有轮动卖出逻辑（不在 Top N 则周一卖出）保留不变。

### 资金管理

止损卖出后当天不买入，等下周一换仓时资金自动参与分配。

### 新增数据列

`10_prepare_data.py` 在 parquet 中新增：
- `atr14`：Wilder ATR(14)，用于追踪止损
- `macd`：EMA(12) - EMA(26)
- `macd_signal`：MACD 的 EMA(9) 信号线

### 运行方式

```bash
cd scripts/bbi/backtrader/v4_plan_1
python -X utf8 10_prepare_data.py
python -X utf8 20_run_backtest.py
```

### 输出文件

与 v4_plan 相同，额外在 `trade_records.csv` 的 `action` 字段中标注止损原因（如"止损卖出(硬止损)"）。
```

---

## 自检：Spec 覆盖验证

| 需求 | 对应 Task |
|------|-----------|
| 复制 v4_plan 全部代码到 v4_plan_1 | Task 1-3（每个文件都从 v4_plan 复制） |
| 卖出逻辑参考 v3（硬止损/ATR/BBI死叉/MACD死叉/筹码胜率） | Task 3 Step 2-6 |
| 本金有限，考虑资金剩余情况买新股 | Task 3 Step 6（止损后当天不买，周一自动分配） |
| 按 v4 规定金额购买 | 原有 alloc 逻辑不变，Task 3 Step 5 |
| 更新 README | Task 4 |
