# ===========================================
# MyStock 数据管理系统 - 配置模板
# ===========================================
# 使用说明:
# 1. 将此文件复制为 .env (去掉.template后缀)
# 2. 根据实际情况修改以下配置
# 3. 将.env文件放在exe同目录下

# --- 数据库配置 ---
# PostgreSQL示例
DATABASE_URL=postgresql://用户名:密码@localhost:5432/数据库名

# MySQL示例 (如果使用MySQL)
# DATABASE_URL=mysql://用户名:密码@localhost:3306/数据库名

# SQLite示例 (如果使用SQLite,无需安装数据库)
# DATABASE_URL=sqlite:///./my_stock.db

# --- Tushare配置 ---
# 在 https://tushare.pro/ 注册获取Token
TUSHARE_TOKEN=你的tushare_token

# --- 可选配置 ---
# 日志级别 (DEBUG/INFO/WARNING/ERROR)
LOG_LEVEL=INFO

# 数据存储路径
DATA_PATH=./data

# 日志配置
LOG_TO_FILE=true
LOG_DIR=./logs
